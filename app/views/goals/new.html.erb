<%= form_with url: goals_path, scope: :goal, local:ture do |form| %>
  <h2 class="row justify-content-center">目標設定ページ</h2>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="mb-3">
        <%= form.label :long_goal, class: "from-label" %>
        
      </div>
    </div>
  </div>

<% end %>